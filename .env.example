# SQLite database (MVP). For production swap to Postgres and update prisma/schema.prisma.
DATABASE_URL="file:./dev.db"

# Where uploaded video files are stored on disk (relative to project root).
VIDEO_STORAGE_DIR="storage/videos"

# Simple admin gate for the upload area (MVP only — replace with real auth later).
ADMIN_PASSWORD="troque-esta-senha"